# ==========================================
# Brick Breaker Tournament - Configuration
# ==========================================

# Server Configuration
PORT=4000
NODE_ENV=development
FRONTEND_URL=http://localhost:3000

# Database (PostgreSQL)
DATABASE_URL=postgresql://user:password@localhost:5432/brickbreaker

# Redis (REQUIRED for production sessions)
# In development, sessions use in-memory storage if not set
REDIS_URL=redis://localhost:6379

# ==========================================
# Lightning Network (LNbits)
# ==========================================
# Get these from your LNbits instance wallet settings

# Invoice/Read key - for creating invoices (receiving payments)
LNBITS_URL=https://legend.lnbits.com
LNBITS_API_KEY=your_invoice_key_here

# Admin key - for sending payouts (KEEP SECRET!)
LNBITS_ADMIN_KEY=your_admin_key_here

# CRITICAL: Webhook signature verification secret
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# Configure this same secret in your LNbits webhook settings
LNBITS_WEBHOOK_SECRET=your_webhook_secret_here

# ==========================================
# Email (Resend – verification & password reset)
# ==========================================
# Get API key from https://resend.com. When set, emails are sent via Resend.
RESEND_API_KEY=your_resend_api_key
# From address – use a verified domain in Resend (e.g. noreply@yourdomain.com)
EMAIL_SENDER=BitBreaker <noreply@yourdomain.com>

# ==========================================
# Tournament Settings
# ==========================================
BUY_IN_SATS=10000

# ==========================================
# LNURL-auth Whitelist System
# ==========================================
# Secret to bootstrap the first admin (one-time use)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# After first admin is created, this can be removed
ADMIN_BOOTSTRAP_SECRET=your_bootstrap_secret_here

# ==========================================
# Security Notes
# ==========================================
#
# PRODUCTION CHECKLIST:
# [ ] Set NODE_ENV=production
# [ ] Set REDIS_URL (required for persistent sessions)
# [ ] Set LNBITS_WEBHOOK_SECRET (required to prevent fake payments)
# [ ] Use strong, unique values for all secrets
# [ ] Never commit this file with real values
#
# GENERATE SECRETS:
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
#
# ==========================================
